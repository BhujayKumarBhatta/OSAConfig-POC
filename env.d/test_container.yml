---

component_skel:
  django_component:
    belongs_to:
      # This is a meta group of a given component type.
      - django_component_all

container_skel:
  django_component_container:
    belongs_to:
      # This is a group of containers mapped to a physical host.
      - django_component-infra_containers
    contains:
      # This maps back to an item in the component_skel.
      - django_component
    properties:
      # These are arbitrary key value pairs.
      service_name: django_component_service

physical_skel:
  # This maps back to items in the container_skel.
  django_component-infra_containers:
    belongs_to:
      - all_containers
  # This is a required pair for the container physical entry.
  django_component-infra_hosts:
    belongs_to:
      - hosts
